import 'package:flutter/material.dart';

const primaryColor = Color(0xff6610f2);

class AppColor{
  static const primary = primaryColor;
  static var white = Colors.white;


  static const catgColor = Colors.blue;
  static const projecColor = Colors.amber;
  static const caisseColor = Colors.green;
  static const settingColor = primaryColor;

}